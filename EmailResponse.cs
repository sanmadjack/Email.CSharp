
namespace Email {
    public enum EmailResponse {
        UserNotFound,
        PasswordNeeded,
        PasswordIncorect,
        CredentialsIncorrect,
        ServerUnreachable,
        ServerReachable,
        EmailSent
    }
}
